return {
  {
    "folke/noice.nvim",
    opts = {
      cmdline = {
        view = "cmdline", -- use classic bottom cmdline instead of popup
      },
      presets = {
        command_palette = false, -- don't show completions as popup
      },
    },
  },
}
