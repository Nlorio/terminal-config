return {
    "folke/snacks.nvim",
    priority = 1000,
    lazy = false,
    opts = {
      -- Enable the terminal feature (required for claudecode.nvim)
      terminal = { enabled = true },
    }
  }
